[package]
name = "colette"
version = "0.1.0"
edition = "2021"

[lib]
name = "colette"
path = "src/lib.rs"

[features]
default = ["memory"]
memory = []
redb = ["dep:redb_crate"]

[dependencies]
redb_crate = { package = "redb", version = "4.1.0", optional = true }
smallvec = "1.15.1"
thiserror = "2.0.18"
